# 📂 Nucleus 2.0 - MultiBot Agents Template

## 🎯 ما هذا المجلد؟

هذا **Template جاهز** لنشر البوتات الذكية التي تتصل بـ **Nucleus Core** عبر بروتوكول **Command & Control**.

---

## 📁 محتويات المجلد

### 🔧 ملفات التشغيل الأساسية
| ملف | الوصف |
|-----|-------|
| `index.js` | نقطة البداية - تشغيل البوت |
| `package.json` | المكتبات المطلوبة |
| `.env.sample` | نموذج لإعدادات البيئة |
| `bot_identity.json` | يُنشأ تلقائياً - هوية البوت (UUID + IP) |

### 🤖 ملفات البوتات الثلاثة
| ملف | نوع البوت | الوصف |
|-----|----------|-------|
| `manager.js` | **Manager** | إدارة العمليات والأوامر |
| `support.js` | **Support** | دعم المستخدمين والمساعدة |
| `maintenance.js` | **Maintenance** | صيانة النظام وتنظيف الذاكرة |

### 🔐 ملفات الأمان والاتصال
| ملف | الوصف |
|-----|-------|
| `core-client.js` | الاتصال بـ Nucleus Core API |
| `control-channel.js` | WebSocket للتحكم والأوامر |
| `identity.js` | توليد UUID وجلب IP تلقائياً |

### 📚 ملفات التوثيق
| ملف | متى تقرأه؟ |
|-----|-----------|
| `README.md` | **ابدأ هنا** - شرح شامل للنظام |
| `QUICK_START.md` | **5 دقائق** - نشر سريع لأول بوت |
| `DEPLOYMENT.md` | **الإنتاج** - نشر احترافي على Replit/PM2 |
| `INDEX.md` | **هذا الملف** - دليل الملفات |

### 🧪 ملفات الاختبار
| ملف | الاستخدام |
|-----|----------|
| `test-connection.js` | اختبر الاتصال بالنواة قبل التشغيل |

---

## 🚀 ابدأ من هنا

### للمبتدئين → `QUICK_START.md`
نشر بوت في **5 دقائق** مع شرح مبسط.

### للمحترفين → `DEPLOYMENT.md`
نشر **18 بوت** (6 منصات × 3 بوتات) على Production.

### للمطورين → `README.md`
تفاصيل تقنية كاملة عن البروتوكول والـ API.

---

## 🎯 كيف تستخدم هذا Template؟

### الطريقة 1: نسخ مباشر
```bash
# لكل منصة، انسخ المجلد
cp -r multibot-agents b2b-manager
cp -r multibot-agents b2b-support
cp -r multibot-agents b2b-maintenance
```

### الطريقة 2: Git Clone (إذا رفعته على GitHub)
```bash
git clone https://github.com/surooh/multibot-agent.git
cd multibot-agent
```

### الطريقة 3: Download ZIP
رفّع هذا المجلد على GitHub، ثم شارك الرابط مع الفريق.

---

## 🔄 دورة حياة البوت

```
1. npm install          → تثبيت المكتبات
2. cp .env.sample .env  → إعداد البيئة
3. node test-connection → اختبار الاتصال
4. npm start            → تشغيل البوت
5. Dashboard            → مراقبة من النواة
```

---

## 🌟 الخطوة التالية

1. **اقرأ** `QUICK_START.md` لبدء أول بوت
2. **اختبر** الاتصال بـ `npm test`
3. **شغّل** البوت بـ `npm start`
4. **راقب** من Nucleus Dashboard

---

**🎉 جاهز للانطلاق! اختر الملف المناسب وابدأ!**

| سرعة | ملف التوجيه |
|------|-------------|
| ⚡ **5 دقائق** | `QUICK_START.md` |
| 🚀 **30 دقيقة** | `DEPLOYMENT.md` |
| 🔬 **ساعة** | `README.md` |
